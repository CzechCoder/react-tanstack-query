// No need to export types, as without imports/exports, the whole file becomes global
type Product = {
  name: string;
  description: string;
  price: number;
  category: string;
  stock: number;
  image_url: string;
};
